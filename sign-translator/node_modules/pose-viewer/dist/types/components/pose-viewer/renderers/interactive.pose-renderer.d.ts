import { PoseRenderer } from "./pose-renderer";
import { PoseBodyFrameModel, PosePointModel, RGBColor } from "pose-format";
import * as THREE from "three";
export declare class InteractivePoseRenderer extends PoseRenderer {
    private scene;
    private camera;
    private renderer;
    private controls;
    private poseGroup;
    private jointGeometry;
    private transform;
    renderJoint(_: number, joint: PosePointModel, color: RGBColor): THREE.Mesh;
    renderLimb(from: PosePointModel, to: PosePointModel, color: RGBColor): THREE.Line;
    private initThree;
    private animate;
    private disposeObject;
    private updateScene;
    render(frame: PoseBodyFrameModel): any;
}
